## Documentation: http://docs.travis-ci.com/user/languages/julia/
language: julia

os:
  - linux
  - osx

julia:
  - 1
  - 1.5
  - nightly

notifications:
  email: false

before_install:
  - git clone --single-branch --branch jeremy/cuda-qf-pointer https://github.com/ceed/libceed
  - pushd libceed
  - make
  - cd lib
  - export JULIA_LIBCEED_LIB=$(find $PWD -name "libceed*")
  - popd

jobs:
  allow_failures:
    - julia: nightly
